proc = Procedure(str rtype, str name, decl* params, stmt* body)
stmt = Statement(expr)
     | Declare(decl lhs, expr rhs)
     | Ret(expr)
expr = Int(int)
     | Str(str)
     | Name(str)
     | Call(str, expr* args)
     | Ternary(expr, expr, expr)
decl = TypedName(str type, str name)
